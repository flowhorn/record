# record Windows

Windows specific implementation for record package called by record_platform_interface.

The old approach using MediaFoundation. It has a high startup latency and compatibility issues on some devices.